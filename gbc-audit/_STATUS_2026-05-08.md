# Status update — 2026-05-08 — after Lovable execution

This file records the state of the 65 GBC issues after Lovable executed `_LOVABLE_PROMPT.md`. It supersedes the `needs-input` framing in the per-issue REPORTs for items that have since shipped.

Last verified main: `cc4cc6d Sequenced T3–T11 updates`. 57 commits since my audit branch (`4223afe`); 23 new migrations on May 5–8, 2026.

## Method

I cannot run `npm test` from this sandbox (registry still blocked, `x-deny-reason: host_not_allowed`). Verification was done by:

1. Static probes mirroring each regression-test's logic against the new migration tree (Python scripts in the verification log).
2. Greps for the prompt's task signatures (RPC names, table names, trigger names).
3. Reading hook source to confirm the wiring of atomic RPCs.

## Lovable's 15-task delivery — actually shipped

| Task | Goal | Status | Verifying artifact |
|---|---|---|---|
| T1 | invoice-assets path-tenancy | ✅ done | `20260507130838_…sql` |
| T2 | denormalise organization_id on detail tables | ⚠️ infra-only | `_sync_org_id_from_parent` trigger fn shipped; the prompt's 9 named tables not all enrolled |
| T3 | optimistic concurrency `version` column | ⚠️ partial | `20260508051926_…sql` covers 12 doc tables (bills, credit_notes, delivery_notes, expenses, goods_receipts, journal_entries, picking_lists, purchase_orders, quotes, sales_orders, stock_adjustments, stock_transfers). NOT covered: profiles, items, salary_structures, financial_records, payroll_records |
| T4 | tenant timezone | ✅ done | `organizations.timezone TEXT NOT NULL DEFAULT 'Asia/Kolkata'` (note: column named `timezone`, not the prompt's `tenant_timezone`) |
| T5 | export_audit_log | ✅ done | `20260508052254_…sql` + `record_export()` RPC |
| T6 | atomic create/update RPCs | ⚠️ 4 of 9 | Done: bills, invoices, sales_orders, purchase_orders. **NOT done: convert_quote_to_invoice (GBC-36), mark_expense_paid (GBC-37), approve_reimbursement (GBC-39), record_payment_receipt (GBC-43), record_vendor_payment (GBC-44), update_purchase_return_with_lines (GBC-61)**. App hooks for the 4 done RPCs are wired (`useInvoices`, `useSalesOrders`, `usePurchaseOrders`, `Bills.tsx`) |
| T7 | status-transition GL/inventory triggers | ❌ misinterpreted | Lovable shipped a `log_status_transition()` trigger that writes to `document_status_transitions` (audit log only). It does NOT post journal entries for credit-note state changes (GBC-40) nor stock_in for delivery-returned (GBC-63) nor sales-return-approved (GBC-64). These remain unfixed |
| T8 | server-side ledger maths | ✅ done | `gl_account_balance(account_id, as_of)` returns debit_total/credit_total/balance, plus `trial_balance_status` likely in same migration |
| T9 | cashflow / analytics aggregations | ⚠️ views-only | Aggregation VIEWS shipped (`v_sales_summary_by_org`, `v_purchases_summary_by_org`, `v_orders_summary_by_org`). The prompt-requested time-series RPCs (`cashflow_monthly_trend(from, to)`, `analytics_monthly_revenue_expense`, `unrealized_fx_pnl`) were NOT shipped — charts still hit the underlying tables |
| T10 | search_vector + GIN/trigram + paginated search RPC | ✅ done | `20260508052807_…sql` |
| T11 | job framework | ✅ done (renamed) | `job_queue` table (Lovable named it `job_queue` instead of the prompt's `job_runs`); cron worker mentioned in commit `7bbb423`. Realtime channel subscription on the frontend is unverified |
| T12 | search_path hardening | ✅ done | `20260507131131_…sql` |
| T13 | stock_adjustment_lines | ❌ not shipped | No `CREATE TABLE … stock_adjustment_lines` in any migration. GBC-58 still broken |
| T14 | vendor delete guard | ✅ done | `20260508052007_…sql` |
| T15 | recurring transactions GL form | ❌ not shipped | `src/pages/financial/RecurringTransactions.tsx` has 0 mentions of `debit_account_id` / `credit_account_id` |

## My 4 regression tests — static-probe results vs current main

| Test | Static probe result | Notes |
|---|---|---|
| `query-key-tenancy.test.ts` (GBC-28) | ✅ 0 declarations missing orgId | F1 fix correctly distinguishes `useQuery` from `invalidateQueries`. EXPECTED_OFFENDERS empty as intended |
| `storage-bucket-policy.test.ts` (GBC-7/15/17) | ✅ 0 post-cutoff FLAT_AUTHENTICATED offenders | Lovable cleared `KNOWN_FLAT_AUTHENTICATED` to empty after invoice-assets migration. F4 regex fragility persists but no false negatives surfaced |
| `security-definer-search-path.test.ts` (GBC-6) | ✅ 0 post-cutoff unpinned offenders | After redeclaration/drop accounting (added by Lovable), every captured fn either pins search_path or has a later pinned redeclaration / explicit DROP |
| `memo-storage-policy.test.ts` (GBC-17) | ✅ unchanged invariants | Source migration `20260218114058` and remediation `20260222035633` both still in tree |

`npm test` cannot run in this sandbox — these are the static equivalents of each test's logic. First real `npm test` after a `vendor/node_modules.tar.gz` is seeded is the moment of truth.

## 65-issue status table (delta from previous index)

| Key | Previous | Now | Why |
|---|---|---|---|
| GBC-1 | needs-input | partial | 4 of 9 atomic RPCs wired; bulk-upload Edge function exists |
| GBC-2 | needs-input | partial | trigger framework exists, table enrolment incomplete |
| GBC-3 | needs-input | partial | same as GBC-2 |
| GBC-4 | needs-input | needs-input | no `v_X_active` views shipped |
| GBC-5 | needs-input | needs-input | architectural; not a Lovable concern |
| GBC-6 | partially-resolved | resolved | T12 migration covers post-cutoff offenders |
| GBC-7 | partially-resolved | resolved | T1 migration; invoice-assets path-tenancy live |
| GBC-8 | needs-input | needs-input | no jsonb_diff audit triggers shipped |
| GBC-9 | needs-input | partial | `organizations.timezone` column shipped; trigger/UI wiring unverified |
| GBC-10 | needs-input | partial | T3 covers 12 of 16 prompt-named tables |
| GBC-11 | needs-input | partial | `job_queue` shipped; realtime subscription unverified |
| GBC-12 | needs-input | needs-input | server-side PDF rendering not shipped |
| GBC-13 | needs-input | resolved | `export_audit_log` table + `record_export` RPC |
| GBC-14 | needs-input | resolved | T10 shipped (FTS + trigram + paginated RPC) |
| GBC-15 | partially-resolved | resolved | invoice-assets policy is now path-scoped |
| GBC-16 | outdated | outdated | unchanged — hook still doesn't exist |
| GBC-17 | partially-resolved | resolved | already correct in main; my regression test pins it |
| GBC-18 | needs-input | needs-input | governance RPC role check unverified |
| GBC-19 | needs-input | needs-input | typed Supabase client unchanged (`as any` casts persist) |
| GBC-20 | needs-input | needs-input | form refactor not shipped |
| GBC-21 | needs-input | needs-input | Settings.tsx still monolithic |
| GBC-22 | needs-input | needs-input | direct supabase calls in components persist |
| GBC-23 | needs-input | needs-input | raw `<button>` tags persist |
| GBC-24 | needs-input | needs-input | Manufacturing Consumption WO-number column not added |
| GBC-25 | partially-resolved | partially-resolved | sub-A resolved by GBC-28; sub-B/C still needs-input |
| GBC-26 | needs-input | needs-input | client.ts not parameterised with `<Database>` |
| GBC-27 | needs-input | needs-input | `select("*")` still rampant |
| GBC-28 | resolved | resolved | (already in my prior commit) |
| GBC-29 | resolved | resolved | (already in my prior commit) |
| GBC-30 | needs-input | needs-input | empty Jira description, still |
| GBC-31 | needs-input | partial | T10 paginated search RPC available; UI wiring unverified |
| GBC-32 | needs-input | partial | `gl_account_balance` available; LedgerExplorer wiring unverified |
| GBC-33 | needs-input | needs-input | hardcoded GSTIN check not relaxed |
| GBC-34 | needs-input | partial | T10 |
| GBC-35 | needs-input | resolved | T14 vendor delete guard |
| GBC-36 | needs-input | needs-input | no `convert_quote_to_invoice` RPC |
| GBC-37 | needs-input | needs-input | no `mark_expense_paid` RPC |
| GBC-38 | needs-input | partial | Bills.tsx wired to atomic RPC; god-component refactor pending |
| GBC-39 | needs-input | needs-input | no `approve_reimbursement` RPC |
| GBC-40 | needs-input | needs-input | T7 misinterpreted — no GL post on credit_note transitions |
| GBC-41 | needs-input | needs-input | same as GBC-40 (vendor side) |
| GBC-42 | needs-input | partial | aggregation views; time-series fn missing |
| GBC-43 | needs-input | needs-input | no `record_payment_receipt` RPC |
| GBC-44 | needs-input | needs-input | no `record_vendor_payment` RPC |
| GBC-45 | needs-input | needs-input | no `unrealized_fx_pnl` RPC |
| GBC-46 | needs-input | partial | Assets pagination via T10 (untested) |
| GBC-47 | needs-input | partial | enabled:!!orgId added by me; aggregations server-side still pending |
| GBC-48 | needs-input | partial | T10 paginated search; UI wiring unverified |
| GBC-49 | needs-input | needs-input | NIC integration still fake — Math.random IRN persists |
| GBC-50 | needs-input | partial | aggregation views; charts still hit raw tables |
| GBC-51 | needs-input | needs-input | RecurringTransactions form lacks GL dropdowns |
| GBC-52 | needs-input | needs-input | automation auto-save not shipped |
| GBC-53 | needs-input | partial | gl_account_balance available; CA dashboard wiring unverified |
| GBC-54 | needs-input | needs-input | snapshot live-revalidation not shipped |
| GBC-55 | needs-input | needs-input | inventory item stock overwrite still allowed |
| GBC-56 | partially-resolved | partially-resolved | hook shipped (mine); UI menu still needs wiring |
| GBC-57 | needs-input | partial | T10 |
| GBC-58 | needs-input | needs-input | stock_adjustment_lines table not created |
| GBC-59 | needs-input | resolved | `update_purchase_order_with_lines` wired |
| GBC-60 | needs-input | needs-input | partial-GRN UI not shipped |
| GBC-61 | needs-input | needs-input | no `update_purchase_return_with_lines` |
| GBC-62 | needs-input | resolved | `create_sales_order_with_lines` wired |
| GBC-63 | needs-input | needs-input | T7 misinterpreted — delivery 'returned' has no stock_in |
| GBC-64 | needs-input | needs-input | sales-return approval has no stock_in / no GL post |
| GBC-65 | needs-input | partial | invoices wired to atomic RPC; pagination via T10 |

### Final tally

| Status | Count | Examples |
|---|---:|---|
| resolved | 11 | GBC-6, 7, 13, 14, 15, 17, 28, 29, 35, 59, 62 |
| partially-resolved | 1 | GBC-56 |
| partial (newly down-graded under post-Lovable definition) | 16 | GBC-1, 2, 3, 9, 10, 11, 25, 31, 32, 34, 38, 42, 46, 47, 48, 50, 53, 57, 65 |
| needs-input | 36 | GBC-4, 5, 8, 12, 18, 19–24, 26, 27, 30, 33, 36, 37, 39, 40, 41, 43, 44, 45, 49, 51, 52, 54, 55, 58, 60, 61, 63, 64 |
| outdated | 1 | GBC-16 |

11/65 fully resolved (17%). 17/65 partial (26%). 36/65 still need explicit follow-up (55%). 1 outdated.

## What still actually blocks closure

1. **Five missing atomic RPCs** (GBC-36/37/39/43/44/61). Same template as the four that shipped — Lovable could do these in one more pass; the prompt is in `_LOVABLE_PROMPT.md` TASK 6.

2. **TASK 7 misinterpretation** (GBC-40/41/63/64). The trigger wired to all header tables logs status changes but does not post the matching journal/stock side effects. Re-issue the prompt for **GBC-40/41**: AFTER UPDATE on `credit_notes` / `vendor_credits` posts a journal entry; **GBC-63/64**: AFTER UPDATE on `deliveries` / `sales_returns` posts `stock_in` to `stock_ledger`.

3. **TASK 13 missed** (GBC-58). `CREATE TABLE public.stock_adjustment_lines` was specified in the prompt but no migration shipped.

4. **TASK 15 missed** (GBC-51). UI form change in `src/pages/financial/RecurringTransactions.tsx` to expose `debit_account_id` / `credit_account_id` dropdowns.

5. **TASK 9 partial** (GBC-42/45/50). Aggregation views were shipped instead of the prompt-requested time-series functions. The CashFlow / Analytics charts still need server-side bucketing functions.

6. **TASK 3 partial coverage** (GBC-10). Five tables from the prompt (profiles, items, salary_structures, financial_records, payroll_records) need the version column.

7. **TASK 6 wiring not pulled into all hooks** — the four delivered RPCs are wired (verified). When the remaining five RPCs land, hooks `useExpenses`, `useReimbursements`, `usePayments`, `useVendorPayments` need rewriting to call them; Quotes.tsx still has a 3-step browser-driven `convertToInvoice` mutation.

8. **GBC-49 (E-invoice IRN)** is not Lovable's job — needs NIC GSP credentials and a paid integration. Treat as a separate workstream.

9. **GBC-19 / GBC-26** (typed Supabase client) — `(supabase as any).rpc(...)` in the wired hooks is a code smell that points at the same underlying issue: client.ts is not parameterised with `<Database>`. Generate types and remove the casts.

10. **`npm test` still has not run anywhere I can verify.** The 4 static-analysis tests would pass per my probes; the broader test suite (whatever else is in `src/test/`) is unverified. `vendor/node_modules.tar.gz` seeding is still the action item from `_SUMMARY.md`.

## Re-issuable Lovable follow-up prompt (compact)

```
Five remaining tasks for the GRX10 ERP, continuing from gbc-audit/_LOVABLE_PROMPT.md:

T6-residual: SECURITY DEFINER RPCs convert_quote_to_invoice(quote_id),
mark_expense_paid(expense_id, payment_method, bank_account_id),
approve_reimbursement(reimbursement_id),
record_payment_receipt(invoice_id, amount, method, bank_account_id),
record_vendor_payment(bill_id, amount, method, bank_account_id),
update_purchase_return_with_lines(id, header, lines). All transactional.
Post journal entries via insert into journal_lines (the trg_sync_financial_records
trigger derives the financial_records row — do NOT write financial_records directly).
Wire the corresponding hooks (useExpenses, useReimbursements, usePayments,
useVendorPayments, Quotes.tsx, PurchaseReturns.tsx).

T7-redo: AFTER UPDATE OF status triggers that POST JOURNAL ENTRIES (not just
log status). On credit_notes: status→'issued' inserts journal_entry+lines for
DR Sales Returns, CR Accounts Receivable; status→'applied' for AR offset
against the original invoice receivable. Mirror for vendor_credits on AP side.
On deliveries: status→'returned' inserts stock_in movements per delivery line
into stock_ledger. Same on sales_returns: status→'approved' inserts stock_in.

T13-redo: CREATE TABLE public.stock_adjustment_lines (id, stock_adjustment_id,
organization_id, item_id, quantity_delta, unit_cost, reason_code,
created_at). RLS scoped via organization_id. Trigger on stock_adjustment_lines
posts to stock_ledger. Update src/pages/inventory/Adjustments.tsx form to
expose a Line Items table.

T15-redo: src/pages/financial/RecurringTransactions.tsx — add two
GL-account dropdowns wired to chart_of_accounts WHERE organization_id =
currentOrg, populating debit_account_id and credit_account_id (already in
the recurring_transactions schema). Both required; reject save without them.

T9-redo: SQL functions returning time-series rows (not just summary views).
cashflow_monthly_trend(from, to) returns (month, inflow, outflow, net).
analytics_monthly_revenue_expense(from, to) returns 12-bucket time series.
unrealized_fx_pnl(as_of) for IAS 21. Wire useCashFlowData, useAnalytics,
useFinancialRecords FX hook.

T3-residual: ADD COLUMN version int NOT NULL DEFAULT 1 + bump_row_version
trigger on profiles, items, salary_structures, financial_records,
payroll_records.

T2-residual: confirm organization_id present on detail tables
salary_components, state_history, document_chains, expense_approvals,
warehouse_bins, payroll_adjustments. Add via _sync_org_id_from_parent
where missing. Update RLS policies on each to use the column directly.
```
